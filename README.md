# moneytime-static

Static assets published by `tzur669-lab/moneytime-app` CI: the Google OAuth
callback page and the signed Android APK. Served via GitHub Pages.

The source app repo is private; this repo intentionally contains no
application code, no source, and no secrets — only these two published files.
